<!DOCTYPE html>
<html lang="en">
<head>
  
  <link rel="stylesheet" href="/css/normalize.css">
  <link rel="stylesheet" href="/css/main.css">
  <link rel="stylesheet" href="/css/bootstrap.css">


  <script src="http://code.jquery.com/jquery-1.9.1.min.js"></script>
  <script src="/js/app.js"></script>
  <script src="/js/bootstrap.js"></script>
  <title>Flashcards</title>

</head>


  <body>
    <div class = "container">
      <div class="navbar navbar-fixed-top">
        <div class="navbar-inner">
          <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </a>
            <a class="brand" href="/">Flashcards</a>
            <div class="nav-collapse">
              <ul class="nav">
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">Browse Decks!<b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    <% @decks = Deck.all %>
                    <% @decks.each do |type| %>
                    <li><a href="/deck/<%= type.id %>"><%= type.name %></a>
                    <% end %>
                  </ul>
                </li>
              </ul>
              <div class="login-signup-btns">
                <% if session[:user_id] %> 
                  <a href = "/logout" class="btn btn-small btn-link">Log out</a>  
                <% else %>
                  <a href = "/login" class="btn btn-small btn-link">Log On!</a>
                <% end %>
                <a href ="/signup" class="btn btn-small btn-warning">Sign Up!</a>
              </div>
            </div><!-- /.nav-collapse -->
          </div><!-- /.container -->
        </div><!-- /.navbar-inner -->
      </div><!-- /.navbar -->
      <%= yield %>
      <div id="footer">
        <div class="container">
          <p class="credits">
            "A Ryan, Katey, Bryan, and J production - &copy; 2013"
          </p>
        </div>
      </div>
    </div>
  </body>

  

</html>
